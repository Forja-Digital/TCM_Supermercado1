@model IEnumerable<TCM_Supermercado1.Models.Produto>

<p>
    <a asp-controller="Produto" asp-action="CadastrarProduto" class="btn btn-primary">Cadastra Produto</a>
</p>

<table class="table custom-table">
    <thead>
        <tr>
            <th>
                <p>Código do produto</p>
            </th>
            <th>
                <p>Categoria</p>
            </th>
            <th>
                <p>Fornecedor(Cnpj)</p>
            </th>
            <th>
                <p>Nome</p>
            </th>
            <th>
                <p>Preço</p>
            </th>
            <th>
                <p>Descrição</p>
            </th>
            <th>
                <p>Quantidade</p>
            </th>
            <th class="text-center">Ação</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var produto in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => produto.cod_produto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => produto.cod_categoria)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => produto.cnpj)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => produto.nome_produto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => produto.preco_produto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => produto.descricao_produto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => produto.quantidade_produto)
                </td>
                @* <td class="text-center"> *@
                @*     <a asp-controller="Produto" asp-action="EditarProduto" asp-route-id="@item.cod_prod" class="btn btn-sm btn-warning">Editar</a> *@
                @*     <span>|</span> *@
                @*     <a asp-controller="Produto" asp-action="ExcluirProduto" asp-route-id="@item.cod_prod" *@
                @*        onclick="return confirm('Deseja realmente Excluir este item?');" class="btn btn-sm btn-danger">Excluir</a> *@
                @* </td> *@
            </tr>
        }

    </tbody>
</table>